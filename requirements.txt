# Dependances directes uniquement : chaque paquet ci-dessous est importe par au
# moins un fichier du depot. Les dependances transitives sont resolues par pip.
#
# La version initiale de ce fichier etait un gel complet de l'environnement
# (59 paquets, dont Faker, GitPython et validators, jamais importes).

# --- Pipeline ---
confluent-kafka==2.3.0      # producteurs et consommateurs Kafka
psycopg2-binary==2.9.9      # PostgreSQL
requests==2.31.0            # appels a randomuser.me
simplejson==3.19.2          # serialisation des messages Kafka
python-dotenv==1.0.1        # lecture du fichier .env

# --- Traitement en continu ---
# La version du connecteur Kafka de Spark est derivee de pyspark.__version__
# dans spark-streaming.py : les deux ne peuvent plus diverger.
pyspark==3.5.0

# --- Tableau de bord ---
streamlit==1.29.0
streamlit-autorefresh==1.0.1
pandas==2.1.3
numpy==1.26.2
matplotlib==3.8.2
